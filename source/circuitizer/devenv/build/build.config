BUILD = True
OPTIMISE = True
REDUCE_SIZE = True
COPY_ASSETS = True
CONSOLE = False

BUILD_PROVIDER = 'nuitka'
PYTHON_POINTER = 'py -3.8'
C_COMPILER = 'msvc=14.2'

RUNTIME_FILE = 'source/boot.py'
TARGET_MAIN_FILE = 'source/mainUI.py'

# This is the optimisation table and setup sheets

LINUX_SETUP_SH = 'build/build.make.linux.ubuntu.sh'
PYTHON_PIP_CONFIG = 'build/build.setup.all.config'
OPTIMISE_SIZE_LIST = 'build/build.optimise.size.all.config'
OPTIMISE_TAR_LIST = 'build/build.optimise.tar.win32.config'

BUILD_ARGS = [
    '--experimental=use_pefile', 'standalone',
    'plugin-enable=tk-inter', 'lto', # 'show-progress',
    'remove-output'
] if BUILD_PROVIDER == 'nuitka' else [
    'noconsole', 'onedir', 'noupx',
    'strip', 'noconfirm', 'clean', 'name "Circuitizer"'
]
